[package]
name = "wisp"
version = "0.1.0"
edition = "2024"

[dependencies]
libghostty-vt = "0.2"
nix = { version = "0.31", features = ["term", "process", "poll", "signal", "ioctl"] }
libc = "0.2"
tokio = { version = "1", features = ["full"] }
clap = { version = "4.6", features = ["derive"] }
